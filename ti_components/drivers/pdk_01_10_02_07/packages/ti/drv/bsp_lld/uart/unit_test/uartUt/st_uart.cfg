var PDK_VPS_COMP_PATH = java.lang.System.getenv("PDK_VPS_COMP_PATH");
xdc.loadCapsule(PDK_VPS_COMP_PATH+"/examples/utility/bspCommonBIOS.cfg");

var GIO          = xdc.useModule('ti.sysbios.io.GIO');
var DEV          = xdc.useModule('ti.sysbios.io.DEV');
var Task         = xdc.useModule('ti.sysbios.knl.Task');
var IntXbar      = xdc.useModule('ti.sysbios.family.shared.vayu.IntXbar');

/* Need to increase from default value of 8 to 16 so that 10 UART instances can
 * be tested - need more for I2C driver as well */
DEV.tableSize = 16;
